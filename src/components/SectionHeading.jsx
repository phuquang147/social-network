import React from 'react'

export default function SectionHeading({heading}) {
    return (
        <h4 className="text-left p-3 text-[13px] font-bold text-gray-500 dark:text-gray-200">{heading}</h4>
    )
}
